query getEpisodes($filter: FilterEpisode) {
  episodes(filter: $filter) {
    info {
      count
      pages
      next
      prev
    }
    results {
      characters {
        id
      }
    }
  }
}
